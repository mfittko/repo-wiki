---
source_repo: "https://github.com/mfittko/repo-wiki"
source_commit: "f3abfc0fc6ecf916c2293708106a5018ea85180d"
page_state: "generated"
source_paths: ["src/prompts.ts"]
compiled_at: "2024-06-01T00:00:00Z"
kind: "module"
confidence: "medium"
claim_status: "source-grounded"
---

# Module `prompts.ts`

## Purpose

This module provides a collection of functions and types related to building and managing prompts, likely for use in documentation generation, code migration, or model-driven contexts. It defines various prompt builders such as `buildArchitecturePrompt`, `buildCrossCuttingPrompt`, `buildFoundationPrompt`, and `buildModulePrompt`, as well as a generic `buildPrompt` function. The module also exports several context and metadata types including `DocCardContext`, `MigrationContext`, `ModelContext`, and `ModuleInfo`. These components suggest the module's role in constructing structured prompts tailored to different architectural or module-related scenarios.

## Source file list

- `src/prompts.ts`

## Key symbols and entry points

- **Functions:**
  - `buildArchitecturePrompt`
  - `buildCrossCuttingPrompt`
  - `buildFoundationPrompt`
  - `buildModulePrompt`
  - `buildPrompt`

- **Types / Interfaces:**
  - `BuiltPrompt`
  - `DocCardContext`
  - `MigrationContext`
  - `ModelContext`
  - `ModuleInfo`

These symbols represent the core API of the module, with prompt-building functions serving as entry points for generating prompts in various contexts, and the types providing structured data models and context information.

## Dependencies and imports

- The source card notes that `src/prompts.ts` has no imports, indicating this module is self-contained and does not depend on external modules or libraries.

## Related tests

- No documentation or source cards indicate the presence of related test files or test coverage for this module.

## Known gaps or open questions

- The exact nature and format of the prompts generated by the builder functions are not detailed here.
- There is no information on how these prompts are consumed or integrated into larger workflows.
- Absence of related tests or documentation leaves the usage and expected behavior of the module somewhat unclear.
- The repository and commit information are unknown, limiting traceability and version context.

<!-- HUMAN_NOTES_START -->
<!-- HUMAN_NOTES_END -->
